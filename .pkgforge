name 'terraform-provider-awscreds'
org 'akerl'

build do
  gopath = tmpdir(:gopath)
  run "go build", 'GOPATH' => gopath
  run "mkdir -p #{releasedir}/usr/bin"
  run "cp terraform-provider-awscreds #{releasedir}/usr/bin/"
end

test do
  # TODO: add tests
end
